version: 2

sources:
  - name: eth
    description: here is my ETH source
    database: eth
    schema: eth_schema
    tables:
      - name: contracts
        description: this is my contacts table
      - name: token_transfers
      - name: transactions
      
  # testing again