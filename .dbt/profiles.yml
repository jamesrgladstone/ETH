eth:
  outputs:
    ci:
      account: HZIVXRS-QE11503
      database: CI
      private_key_path: "{{ env_var('DBT_PRIVATE_KEY_PATH') }}"
      role: ACCOUNTADMIN
      schema: CI_SCHEMA
      threads: 1
      type: snowflake
      user: jimgladstone
      warehouse: COMPUTE_WH
      
  target: ci